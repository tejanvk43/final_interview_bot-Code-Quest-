rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow public access for this demo application
    // WARNING: For production, you must add authentication
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
